{
  "name": "auto-assistant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "./ai-handler",
    "./db-handler",
    "./push-handler",
    "./app",
    "./hub"
  ],
  "scripts": {
    "dev:app": "cd consumer && pnpm dev",
    "dev:hub": "cd hub && pnpm dev",
    "dev": "pnpm app && pnpm hub",
    "shutdown:app": "kill-port --port 3001",
    "shutdown:hub": "kill-port --port 3000",
    "shutdown": "kill-port --port 3001,3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
